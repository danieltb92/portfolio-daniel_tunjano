---
import ProjectCard from '../components/Card.astro';
import projectsData from '../data/projects.json';

interface ProjectData {
    title: string;
    slug: string;
    image: {
        src: string;
        alt: string;
    };
    tags: string[];
    link: {
        href: string;
        text: string;
        external: boolean;
    };
}

const projects: ProjectData[] = projectsData;
---

<div class="flex justify-center flex-col items-center p-0 md:p-16">
    <h2 class="text-3xl font-bold mb-20 text-center">Proyectos</h2>
    <div class="grid grid-cols-1 gap-6 max-w-6xl w-[80%]">
        {projects.map((project) => (
            <ProjectCard
                class="w-full mb-10"
                title={project.title}
                slug={project.slug}
                image={project.image}
                tags={project.tags}
                link={project.link}
                variant="minimal"
            />
        ))}
    </div>
</div>