---
import Scroller from "@/components/ui/Scroller.astro"
import Figma from '@/assets/tools/figma.svg';
import Illustrator from '@/assets/tools/illustrator.svg';
import Photoshop from '@/assets/tools/photoshop.svg';
import Blender from '@/assets/tools/blender.svg';
import React from '@/assets/tools/React_light.svg';
import Git from '@/assets/tools/git.svg';
import Tailwind from '@/assets/tools/tailwindcss.svg';
import AstroLogo from '@/assets/tools/Astro_dark.svg';

import type { ImageLayout } from "node_modules/astro/dist/assets/types";
import type { SvgComponent } from "astro/types";
---
<section class="flex justify-center flex-col max-w-4xl m-auto">
  <!-- <h2 class="!text-4xl font-bold text-center mb-6">
    Herramientas y Tecnologías
  </h2> -->
  <Scroller 
  logos={[
    { src: Figma, alt: 'Figma' },
    { src: Illustrator, alt: 'Illustrator' },
    { src: Photoshop, alt: 'Photoshop' },
    { src: Blender, alt: 'Blender' },
    { src: React, alt: 'React' },
    { src: Git, alt: 'Git' },
    { src: Tailwind, alt: 'Tailwind CSS' }
    , { src: AstroLogo, alt: 'Astro'},
    // { src: NodeJS, alt: 'Node.js' },
    // { src: Git, alt: 'Git' },
    // { src: Github, alt: 'GitHub' },
  ]}
  grayscale={false}
  speed={'normal'}
  aria-Label="Logo marquee"
/>
</section>




<!-- ---
export interface LogoItem { src: string; alt?: string }
export interface Props {
  logos?: LogoItem[]
  duration?: number
  ariaLabel?: string
}

const { logos = [  { src: '/assets/logos/facebook.svg', alt: 'Facebook' },
  { src: '/assets/logos/disney.svg', alt: 'Disney' },
  { src: '/assets/logos/airbnb.svg', alt: 'Airbnb' },
  { src: '/assets/logos/spotify.svg', alt: 'Spotify' },], duration = 25, ariaLabel = 'Logo marquee' } = Astro.props as Props
---

<div
  class="w-full inline-flex flex-nowrap overflow-hidden 
         [mask-image:_linear-gradient(to_right,transparent_0,_black_128px,_black_calc(100%-128px),transparent_100%)]"
  role="region"
  aria-label={ariaLabel}
>
  <ul
    id="logos"
    class="flex items-center justify-center [&_li]:mx-8 [&_img]:max-w-none animate-infinite-scroll"
    style={`--marquee-duration:${duration}s`}
  >
    {logos.map((logo) => (
      <li>
        <img
          src={logo.src}
          alt={logo.alt ?? ''}
          loading="lazy"
          class="h-8 md:h-10 lg:h-12 object-contain"
        />
      </li>
    ))}
  </ul>
</div>

<script is:inline>
  document.addEventListener('DOMContentLoaded', () => {
    const ul = document.getElementById('logos');
    if (!ul || ul.nextElementSibling?.getAttribute('aria-hidden') === 'true') return;
    const clone = ul.cloneNode(true);
    clone.setAttribute('aria-hidden', 'true');
    ul.after(clone);
  });
</script>

<style>
  /* 🎞️ Animación local del marquee */
  @keyframes infinite-scroll {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-100%);
    }
  }

  .animate-infinite-scroll {
    animation: infinite-scroll var(--marquee-duration, 25s) linear infinite;
  }
</style> -->
